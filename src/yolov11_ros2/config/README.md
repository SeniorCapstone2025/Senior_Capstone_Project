# YOLOv11 Config

Place model weights here, or they will be downloaded automatically at runtime.

Default model: `yolo11n.pt` (YOLOv11 nano, COCO pretrained)

The ultralytics library will auto-download if not present.
